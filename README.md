# shenzhi

乳腺癌副作用自评类 MVP 单仓：前端 `apps/web`、后端 `apps/api`；文档见 `docs/`。

- **目录约定（与最新架构对齐）**：[`docs/Directory-Structure.md`](docs/Directory-Structure.md)（四类介质 **D / E / K / FS**、`storage/`、`jobs/handlers/`、`data/sessions` 三层与 `data/experience` 分区）。
- 各目录下的 `agent.md` 为占位说明，实现阶段可按 `Directory-Structure.md` 替换为真实源码文件。
